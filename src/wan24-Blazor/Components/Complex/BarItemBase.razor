@inherits ParentComponentBase
@implements IBarItemParametersExt
@implements IMenuItemComponentHost
@implements IMenuParentItem

@if(IsVisible)
{
    <CascadingValue Value="this">
        <DynamicComponent Type=@Type Parameters=@Parameters/>
    </CascadingValue>
}
