@inherits DisposableBoxBase
@implements IBarParametersExt
@inject NavigationManager Navigation

@if (IsVisible)
{
    <SectionContent SectionName=@UseBoxSection>
        <CascadingValue Value="this">
            @ChildContent
        </CascadingValue>
    </SectionContent>
    base.BuildRenderTree(__builder);
}
